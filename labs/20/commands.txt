# ln file link1		создать hardlink
# ln -s file link1	создать symlink
# ls -l можно посмотреть количество связей

# find /path/to/bin -name (-iname, -type) '*.txt' --max-depth=1

# xargs -o (stdin -> args)
# find ... | xargs ls -l		->		ls -l file1 file2 file3 ...

20 утилит разобрать, 3 из списка - подробно
# tar -c ... | tee >(md5sum > sum.txt) | ssh ...

# cat
# tee
# sum
# md5sum
# sha1sum
! awk
# wc
# gzip, bzip
# od, xxd
# du, df -h
? dim
# file
# cmp
!? vi/vim
sed
split
diff + patch
sort
join
cut
paste
tr
mktemp
comm
dd
git

-- Сравни с тетрадью
-- Отбери 3 самых интересных утилиты узнай о них
-- Сделай 1 ую лабу

/etc/passwd
/etc/group
/etc/shadow

#Не работает :(
cut -f 1,4 -d ':' /etc/passwd > gd.txt
sort -k 2 -t ":" -n gd.txt > sorted.txt 
cut -f 1,3 -d ':' /etc/group > dg.txt
sort -k 2 -t ":" -n dg.txt > gorted.txt
join -t ':' -j 2 -o 2.1 1.1 gorted.txt sorted.txt